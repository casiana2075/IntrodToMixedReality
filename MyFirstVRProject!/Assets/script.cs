using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class script : MonoBehaviour
{
    // Start is called before the first frame update

    /*private void Awake() //preprocesare inainte de start a unui obiect
    {
        Debug.Log("Awake");
    }
  
    void Start()
    {
        
    }

    private void FixedUpdate() // 
    {
        car.position += new Vector3(0, 0, 1);
    }

    // Update is called once per frame
    void Update()
    {
        car.position += new Vector3(0, 0, 1) * Time.deltaTime; // la 60 fps o sa mearga cu 60 de metrii pe secunda  dar cu delta time o sa mearga egal pe toate device urile
    }

    private void OnDestroy() // 
    {
        Debug.Log("Destroy");
    }

    private void OnEnable()  
    {
        Debug.Log("Enable");
    }

    //util la tema asta
    private void OnTriggerEnter(Collider other)
    {
        other.gameobject.getcomponent<Rigidbody>().AddForce(new Vector3(0, 0, 1) * 1000);
    }

    private void OnCollisionEnter(Collision collision)
    {
        
    }

    private void OnTriggerStay(Collider other)
    {
        
    }

    private void OnTriggerExit(Collider other)
    {
        
    }*/


}
